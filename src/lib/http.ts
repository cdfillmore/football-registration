import { createHmac, timingSafeEqual } from 'node:crypto';
export function signed(value: string, secret = process.env.SESSION_SECRET ?? 'dev-secret') { const sig = createHmac('sha256', secret).update(value).digest('base64url'); return `${value}.${sig}`; }
export function validCookie(value: string | undefined, secret = process.env.SESSION_SECRET ?? 'dev-secret') { if (!value) return false; const [v, s] = value.split('.'); if (!v || !s) return false; const expected = signed(v, secret).split('.')[1]; return s.length === expected.length && timingSafeEqual(Buffer.from(s), Buffer.from(expected)); }
export function originOk(request: Request, expected = process.env.ORIGIN ?? import.meta.env.ORIGIN) { const origin = request.headers.get('origin'); return !import.meta.env.PROD || !expected || origin === expected; }
